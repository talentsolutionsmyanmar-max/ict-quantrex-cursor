[pytest]
markers =
    ict: ICT / pipeline compatibility regression tests (Phase 0 gate).
    data_pipeline: CryptoNativeDataPipeline unit tests (Phase 0 data layer).
    trend_network: TrendNetwork (Phase 1 optional).
    hybrid_scorer: Hybrid score fusion (Phase 1 optional).
    scoring_hook: HybridScoringHook integration (Phase 1 optional).
